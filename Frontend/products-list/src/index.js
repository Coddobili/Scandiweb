import React from 'react';
import ReactDOM from 'react-dom/client';
import $ from 'jquery';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot($('#root'));
root.render(
    <App />
);
